import numpy as np
# Set the base array
arr = np.arange(0,11)
print(arr)

# Can perfome simple arithmatic operaitons in arrays
print(arr + arr)
# Scalar operations
print(arr * 10)

# Universal array functions

# Calculating the square root
print(np.sqrt(arr))

# Calculating the max element
print(np.max(arr))